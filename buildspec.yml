version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket deliwebcrossact --output-template-file outputSamTemplate.yaml
      #- aws s3 cp deliforce-server11-10-18.yaml s3://swaggerwebfinalapi/deliforce-server11-10-18.yaml
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputSamTemplate.yaml
